# Vendored and trimmed for Stitch.
# Trimmed: the e2e test suite and its
# dev-dependencies were dropped; the in-crate unit tests are kept.
[package]
name = "audio-sync"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
realfft = { workspace = true }
