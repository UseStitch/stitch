# Vendored and trimmed for Stitch.
# Trimmed to the dynamic-rate chunk
# resampler used by capture; the legacy/static resampler variants were dropped.
[package]
name = "stitch-resampler"
version = "0.0.1"
edition = "2024"
license = "MIT"

[dependencies]
audioadapter-buffers = "2.0"
futures-util = { workspace = true }
pin-project = { workspace = true }
rubato = "1.0.1"
thiserror = { workspace = true }
