export type { ModelSelection as ModelSpec } from '@/components/model-selectors/model-combobox';

export type Attachment = { id: string; path: string; previewUrl: string | null; mime: string; filename: string };
